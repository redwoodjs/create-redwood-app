{
  "private": true,
  "name": "api",
  "version": "0.0.0",
  "dependencies": {
    "@hammerframework/api": "^0.0.1-alpha.13.2",
    "@generated/photon": "0.0.0-generated"
  },
  "devDependencies": {
    "@hammerframework/hammer-dev-server": "^0.0.1-alpha.12",
    "prisma2": "2.0.0-preview-12"
  },
  "scripts": {
    "dev": "yarn hammer-dev-server",
    "db:up": "yarn prisma2 lift up && yarn prisma2 generate",
    "db:save": "yarn prisma2 lift save",
    "build": "NODE_ENV=production yarn workspace api db:up && NODE_ENV=production babel src --out-dir dist",
    "postinstall": "prisma2 generate"
  }
}
