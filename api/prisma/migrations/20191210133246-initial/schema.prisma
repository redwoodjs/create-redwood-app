datasource hammerDatasource {
  provider = "sqlite"
  url = "***"
}

generator photonjs {
  provider = "photonjs"
}

// Define your own datamodels here and run `yarn db:save` to create
// migrations for them.

model User {
  id Int @id
  email String @unique
}
