# Migration `20191101214352-initial-migration`

This migration has been generated by Peter Pistorius at 11/1/2019, 9:43:52 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "lift"."User" (
  "email" TEXT NOT NULL DEFAULT ''  ,
  "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT ,
  "isAdmin" BOOLEAN NOT NULL DEFAULT false  ,
  "name" TEXT    
);

CREATE UNIQUE INDEX "lift"."User.email" ON "User"("email")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20191101214352-initial-migration
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,20 @@
+datasource hammerDatasource {
+  provider = env("PRISMA_DS_PROVIDER") // sqlite
+  url = env("PRISMA_DS_URL") // "file:./dev.db"
+}
+
+generator photonjs {
+  provider = "photonjs"
+  output = "../../packages/photon"
+  binaryTargets = ["darwin", "linux-glibc-libssl1.0.2"]
+}
+
+// Define your own datamodels here and run `yarn db:save` to create
+// migrations for them.
+
+model User {
+  id Int @id
+  name String?
+  email String @unique
+  isAdmin Boolean @default(false)
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20191101214352-initial-migration)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191101214352-initial-migration'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
