# Migration `20191121124859-make-username-optional`

This migration has been generated by Peter Pistorius at 11/21/2019, 12:48:59 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "lift"."new_User" (
  "email" TEXT NOT NULL DEFAULT ''  ,
  "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT ,
  "name" TEXT    ,
  "username" TEXT    
);

INSERT INTO "new_User" ("email","id","name","username") SELECT "email","id","name","username" from "User"

DROP TABLE "lift"."User";

ALTER TABLE "lift"."new_User" RENAME TO "User";

CREATE UNIQUE INDEX "lift"."User.email" ON "User"("email")

CREATE UNIQUE INDEX "lift"."User.username" ON "User"("username")

PRAGMA "lift".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20191121124859-make-username-optional
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,20 @@
+datasource hammerDatasource {
+  provider = "sqlite"
+  url = "file:./dev.db"
+}
+
+generator photonjs {
+  provider = "photonjs"
+  output = "../../packages/photon"
+  binaryTargets = ["darwin", "linux-glibc-libssl1.0.2"]
+}
+
+// Define your own datamodels here and run `yarn db:save` to create
+// migrations for them.
+
+model User {
+  id Int @id
+  name String?
+  email String @unique
+  username String? @unique
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20191121124859-make-username-optional)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191121124859-make-username-optional'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
