datasource DS {
  provider = "sqlite"
  url = "file:./dev.db"
}

generator photonjs {
  provider = "photonjs"
}

// Define your own datamodels here and run `yarn db:save` to create
// migrations for them.
